# Must match how you open the frontend (use localhost, not 127.0.0.1)
VITE_API_URL=http://localhost:8010

# Legacy Node/Drizzle (optional; unused when FastAPI is the API)
DATABASE_URL=mysql://root:password@127.0.0.1:3306/skyra
SESSION_SECRET=change-me-to-a-long-random-string
ADMIN_EMAIL=admin@skyrarealty.local
ADMIN_PASSWORD=skyra2026
