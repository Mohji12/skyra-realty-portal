# FastAPI MySQL connection (SQLAlchemy)
DATABASE_URL=mysql+pymysql://user:password@127.0.0.1:3306/skyra

# Comma-separated browser origins allowed to call the API with cookies
CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,https://skyra-realty-portal.vercel.app

SESSION_COOKIE_NAME=skyra_session
SESSION_DAYS=7

# Seeded by: python -m app.seed
ADMIN_EMAIL=admin@skyrarealty.local
ADMIN_PASSWORD=skyra2026
